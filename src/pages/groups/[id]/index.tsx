/**
 * Group detail redirect page — Framework layer (thin re-export)
 * All presentation logic lives in src/presentation/features/groups/
 */
export { GroupRedirect as default } from '../../../presentation/features/groups.feature';
