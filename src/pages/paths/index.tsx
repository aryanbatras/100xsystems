/**
 * Paths page — Framework layer (thin re-export)
 * All presentation logic lives in src/presentation/features/paths/
 */
export { PathsPage as default } from '../../presentation/features/paths.feature';
