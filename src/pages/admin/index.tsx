/**
 * Admin page — Framework layer (thin re-export)
 * All presentation logic lives in src/presentation/features/admin/
 */
export { Admin as default } from '../../presentation/features/admin.feature';
